# Environment Variables Example
# Copy this file to .env for local development

# Database URL (Railway PostgreSQL)
# Railway automatically provides this in production
# For local development, use SQLite or your own PostgreSQL
DATABASE_URL=sqlite:///./fincount.db

# For Railway PostgreSQL (example):
# DATABASE_URL=postgresql://postgres:password@host:port/railway

# JWT Secret Key (IMPORTANT: Generate a new one for production!)
# Generate a secure key with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=Kihqm8wAXCAZXMCba4T61IMpsqrVvjV0bbjTBQKqH3U

# Server Configuration (Railway provides PORT automatically)
PORT=8000
HOST=0.0.0.0
